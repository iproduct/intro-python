# pyproject.toml
[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra --strict-markers"
testpaths = [
    "tests",
]
markers =[
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "serial",
    ]